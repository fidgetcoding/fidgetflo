# swarm-monitor

Real-time swarm monitoring.

## Usage
```bash
npx fidgetflo swarm monitor [options]
```

## Options
- `--interval <ms>` - Update interval
- `--metrics` - Show detailed metrics
- `--export` - Export monitoring data

## Examples
```bash
# Start monitoring
npx fidgetflo swarm monitor

# Custom interval
npx fidgetflo swarm monitor --interval 5000

# With metrics
npx fidgetflo swarm monitor --metrics
```
